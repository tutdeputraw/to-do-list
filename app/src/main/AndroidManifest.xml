<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tutdeputraw.todolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList">
        <activity
            android:name=".toDoList.EditPopupActivity"
            android:theme="@style/Theme.ToDoList.Popme" />
        <activity
            android:name=".toDoList.AddPopupActivity"
            android:theme="@style/Theme.ToDoList.Popme" />
        <activity android:name=".historyList.HistoryActivity" />
        <activity android:name=".toDoList.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>